import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

// TYPES

// STATE
const state = {
}

// MUTATIONS
const mutations = {
}

// ACTIONS
const actions = ({
})

export default new Vuex.Store({
  state,
  mutations,
  actions
})
